spring:
  application:
    name: sample-remote-command-proj

  datasource:
    url: jdbc:postgresql://localhost:5432/remote_command
    username: admin
    password: ${DB_PASSWORD:admin123}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false

  security:
    user:
      name: admin
      password: admin

server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always

# Application specific configuration
app:
  security:
    admin-api-key: ${ADMIN_API_KEY:admin-master-key-change-in-production}

  ssh:
    timeout-seconds: 60
    connect-timeout-seconds: 10
    retry:
      max-attempts: 3
      backoff-ms: 1000

  command:
    whitelist-path: classpath:whitelist.txt
    blacklist-path: classpath:blacklist.txt
    policy-reload-enabled: true
    policy-check-interval-ms: 5000

  encryption:
    # Use environment variable in production: ENCRYPTION_KEY
    secret-key: ${ENCRYPTION_KEY:default-encryption-key-change-me-in-production}

# Logging
logging:
  level:
    root: INFO
    kr.urock.sample_remote_command_proj: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
